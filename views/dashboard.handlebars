{{#each posts as |post| }}
<div class="post-card">
  <div class="post-head">
    <h2>{{post.title}}</h2>
    <h3>{{post.user}}</h3>
  </div>
  <div class="post-body">
    <p>{{post.body}}</p>
  </div>
  <div class="post-date">
    <p>{{format_date post.date}}</p>
  </div>
</div>
{{/each}}
